FEATURES:

DONE -- start game by game master

DONE -- when scores are evaluated, lock other actions except selected

show score resolution after evaluating

split create join panel to write name after room join

DONE -- dont create room on join

last player should select opponent

DONE -- timers

streaks (and breaks) ui + calculations

win lose conditions + ui

leave and rejoin functionality
DONE -- custom unique device identifier (doesn't store actual device info, that may be troublesome due to GDPR)

==============================================

BUGS:
after first round, unlock the action buttons

when checking if every player has selected action, make sure that the player "isPlaying == true"

player not playing in current round shouldnt see the actions screen